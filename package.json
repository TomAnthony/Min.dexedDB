{
  "name": "MinimalDexDB",
  "version": "0.0.1",
  "description": "A tiny wrapper for IndexedDB, which just supplies put() and get(), with Promises. Less than 1kb.",
  "repository": "git@github.com:TomAnthony/MinimalDexDB.git",
  "author": "tom@tomanthony.co.uk",
  "license": "MIT",
  "scripts": {
    "build": "./node_modules/uglify-es/bin/uglifyjs --mangle -c drop_console=true minimaldexdb.js -o minimaldexdb.min.js"
  },
  "dependencies": {
    "uglify-es": "^3.3.10"
  }
}
